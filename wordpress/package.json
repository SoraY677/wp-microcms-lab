{
  "name": "wordpress",
  "version": "1.0.0",
  "scripts": {
    "dev": "docker-compose up",
    "bash-wp": "docker-compose exec wordpress /bin/bash",
    "bash-db-root": "docker-compose exec db mysql -u root -p db",
    "bash-db-user": "docker-compose exec db mysql -u root -p db"
    
  },
  "license": "MIT"
}
